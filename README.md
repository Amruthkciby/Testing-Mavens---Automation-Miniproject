# Automation Miniproject

Run the project (requires Node.js >= 12):

```bash
npm run start
```

Files:
- `app.js` — main entry that selects players and logs teams
- `testdata.json` — sample players data
- `utils/playerSelector.js` — filters players (age >= 18) and builds teams
- `models/` — class definitions for `Allrounder`, `Batter`, `Bowler`, `Team`
